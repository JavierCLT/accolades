name: Merrill Edge Daily Monitor

on:
  schedule:
    - cron: "15 12 * * *"
  workflow_dispatch:
    inputs:
      baseline:
        description: "Store current results as seen without sending email"
        required: false
        default: false
        type: boolean

permissions:
  contents: read

jobs:
  monitor:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Set cache date
        id: cache-date
        run: echo "date=$(date -u +%Y-%m-%d)" >> "$GITHUB_OUTPUT"

      - name: Restore SQLite state
        uses: actions/cache@v4
        with:
          path: data/monitor.sqlite
          key: ${{ runner.os }}-merrill-monitor-sqlite-${{ steps.cache-date.outputs.date }}-${{ github.run_id }}
          restore-keys: |
            ${{ runner.os }}-merrill-monitor-sqlite-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e .[llm]

      - name: Run monitor
        env:
          DB_PATH: data/monitor.sqlite
          LOG_LEVEL: INFO
          LOCAL_TIMEZONE: America/New_York
          SEND_EMPTY_DIGEST: "false"
          MAX_ITEMS_PER_DIGEST: "30"
          BRAVE_SEARCH_API_KEY: ${{ secrets.BRAVE_SEARCH_API_KEY }}
          EMAIL_BACKEND: ${{ secrets.EMAIL_BACKEND }}
          EMAIL_FROM: ${{ secrets.EMAIL_FROM }}
          EMAIL_TO: ${{ secrets.EMAIL_TO }}
          SMTP_HOST: ${{ secrets.SMTP_HOST }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_USERNAME: ${{ secrets.SMTP_USERNAME }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
          SMTP_STARTTLS: ${{ secrets.SMTP_STARTTLS }}
          GMAIL_TOKEN_JSON: ${{ secrets.GMAIL_TOKEN_JSON }}
          LLM_CLASSIFIER_ENABLED: ${{ secrets.LLM_CLASSIFIER_ENABLED }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_MODEL: ${{ secrets.OPENAI_MODEL }}
          OPENAI_REASONING_EFFORT: low
          BASELINE_RUN: ${{ github.event_name == 'workflow_dispatch' && inputs.baseline == true }}
        run: |
          if [ "$BASELINE_RUN" = "true" ]; then
            merrill-monitor run --sources sources.yaml --queries queries.yaml --baseline
          else
            merrill-monitor run --sources sources.yaml --queries queries.yaml
          fi
